@echo off
REM Copia este archivo como start-page.local.env.cmd (mismo directorio) y rellena valores.
REM start-page.local.env.cmd no debe subirse a git (esta en .gitignore).

REM Solo esta PC (por defecto el servidor ya usa 127.0.0.1). Para permitir LAN:
REM set HOST=0.0.0.0

REM Proteger la pagina y /dashboard con usuario y contrasena (HTTP Basic):
REM set START_PAGE_AUTH_USER=tu_usuario
REM set START_PAGE_AUTH_PASS=tu_contrasena_segura
